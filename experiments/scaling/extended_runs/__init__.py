"""Long-token ToolCall scaling experiments.

This package is intentionally a sibling of ``scaling_runs``.  It owns the two
extended experiments while reusing the proven shard loader, checkpoint format,
metrics logger, and utilities from the baseline scaling package.
"""

