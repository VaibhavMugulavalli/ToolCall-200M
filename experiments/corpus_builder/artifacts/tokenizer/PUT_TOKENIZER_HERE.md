# Required tokenizer

Copy the frozen native SentencePiece model here before building data:

`toolcall_spm_32k.model`

The generator verifies that it has exactly 32,000 pieces and copies it into the
finished data bundle. The model file is intentionally not included because it
was not part of the supplied attachments.
